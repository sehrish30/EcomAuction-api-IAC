import { Construct } from "constructs";

interface EcomAuctionLambdaProps {}

export class EcomAuctionLambda extends Construct {
  constructor(scope: Construct, id: string, props: EcomAuctionLambdaProps) {
    super(scope, id);
  }
}
