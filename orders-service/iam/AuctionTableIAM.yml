AuctionsTableGetAuctionsIAM:
  Effect: Allow
  Action:
    - dynamodb:Query
  Resource:
    - ${self:custom.AuctionsTable.arn}

AuctionsTableUpdateAuctionsIAM:
  Effect: Allow
  Action:
    - dynamodb:Update
  Resource:
    - ${self:custom.AuctionsTable.arn}
