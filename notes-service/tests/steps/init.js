// cognito user pool id, client id, username , password
// load these values from environment

"use strict";

const init = async () => {
  // load variables in environment variable process
  require("dotenv").config();
};

module.exports = init;
